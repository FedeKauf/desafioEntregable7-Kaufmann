<h2>Login de usuarios</h2>
<hr>
<form class="container mb-3" action="/api/sessions/login" method="post">
  {{#if error}}
  <div class="error">{{errorDetalle}}</div>
  {{/if}}

  {{#if usuarioCreado}}
  <div class="success">Se ha creado correctamente el usuario {{usuarioCreadoDetalle}}</div>
  {{/if}}

  <input type="email" name="email" placeholder="email">
  <input type="password" name="password" placeholder="password">

  <input type="submit" value="Login">
</form>

<br>
{{>menu}}
  <hr>
  <br>
  <a href="/api/sessions/github" class="container mb-3">Entrar con GitHub</a>